import '../../src/src.dart';

class ThumbScanning extends StatelessWidget {
  const ThumbScanning({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Thumb scanning is comming soon"),),
    );
  }
}
